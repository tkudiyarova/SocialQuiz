<%= form_for(@question) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :title, placeholder: "Compose new question..." %>
  </div>
  <%= f.submit "Add question", class: "btn btn-large btn-primary" %>
<% end %>